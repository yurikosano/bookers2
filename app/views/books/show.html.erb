<div class="container">
    <div class="row">
        <div class="col-md-8 offset-md-1">
         <h2>Book detail<h2>
            <table class="table">
                <tbody>
                    <tr>
                    <td><%= image_tag @user.get_profile_image(40,40) %>
                        <br />
                        <%= link_to @user.name, user_path(@user) %></td>
                    <td><%= link_to @book.title, book_path(@book) %></td>
                    <td><%= @book.opinion %></td>
                    </tr>
                </tbody>
            </table>
        </div>
    </div>
</div>

